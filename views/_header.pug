header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All tours
  .header__logo
    a(href='/')
      img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    button.theme-toggle#theme-toggle(aria-label='Toggle dark mode')
      .theme-toggle__slider
      span.theme-toggle__icon.theme-toggle__icon--sun ☀️
      span.theme-toggle__icon.theme-toggle__icon--moon 🌙
    if user
      if user.role === 'admin' || user.role === 'lead-guide'
        a.nav__el(href='/admin/dashboard') Admin
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up